# Secrets are read from git-ignored local files (never committed).
RAILS_MASTER_KEY=$(cat config/master.key)
KAMAL_REGISTRY_PASSWORD=$(cat .kamal/registry_password)
POSTGRES_PASSWORD=$(cat .kamal/postgres_password)
NEXUS_SHINE_DATABASE_PASSWORD=$(cat .kamal/postgres_password)
